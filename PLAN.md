# DeskSnapshot 产品需求与实施计划

## 产品目标

为 Windows 用户提供一个轻量、离线、可撤销的桌面图标位置备份工具，在分辨率变化、显示器切换或图标意外重排后快速恢复布局。

## 功能需求

### P0：第一版 Demo

- 读取当前桌面图标名称及 X/Y 坐标
- 以物理像素记录虚拟桌面分辨率，并记录系统 DPI、显示器数量和备份时间
- 用户可命名备份，备份数据持久化到本地 JSON
- 展示历史备份列表、详情和图标数量
- 将仍然存在的同名图标恢复到备份坐标
- 恢复前自动创建当前布局的安全备份
- 支持删除历史备份
- 提供操作成功、部分成功和失败反馈
- WinUI 3 Fluent 界面，支持浅色、深色及跟随系统主题

### P1：可用性增强

- 备份布局缩略图与恢复前预览
- 当前布局与备份差异对比
- 分辨率/DPI 不一致时按比例适配
- 单独选择要恢复的图标
- 系统托盘与一键快速备份
- 自动备份与保留策略
- JSON 备份导入、导出及版本迁移

### P2：完整产品能力

- 精确的多显示器设备匹配与布局映射
- 显示器连接、断开和分辨率变化事件监听
- 开机启动及后台低资源常驻
- 跨设备布局迁移
- 高对比度、键盘导航和本地化完善
- MSIX 打包、签名与自动更新

## 第一版技术方案

- UI：WinUI 3 / Windows App SDK 2.2，单窗口导航布局
- 运行方式：unpackaged、x64、自包含 Windows App SDK
- 桌面访问：Win32 查找 `SHELLDLL_DefView/SysListView32`，通过跨进程内存读取图标文本与坐标
- 数据：`System.Text.Json`，保存到 `%LOCALAPPDATA%\DeskSnapshot\backups.json`
- 恢复匹配：按图标显示名称匹配；重复名称按出现顺序处理
- 安全策略：任何人工恢复前都捕获并保存一份安全备份

## 里程碑

1. 工程骨架、主题资源与主窗口
2. 桌面图标捕获和系统环境读取
3. JSON 备份仓储及历史列表
4. 布局恢复、安全备份和结果反馈
5. 编译验证、使用说明和 Git 基线

## Demo 验收标准

- 工程可通过 `dotnet build -p:Platform=x64` 编译
- 点击“立即备份”后，历史列表新增一条真实桌面布局记录
- 重启应用后历史记录仍存在
- 选择记录可执行恢复，且操作前生成安全备份
- 缺失图标不会导致整体恢复失败，界面会显示部分成功结果

## 已知限制

- Demo 仅验证 Windows Explorer 默认桌面，第三方桌面外壳不在支持范围
- 图标唯一性暂按显示名称判断；同名图标的稳定匹配将在后续增强
- 多显示器仅记录环境信息，尚未进行显示器级坐标重映射
- 若启用了 Windows“自动排列图标”，Explorer 可能覆盖恢复的位置
