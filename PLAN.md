# DeskSnapshot 产品需求与实施计划

## 产品目标

为 Windows 用户提供一个轻量、离线、可撤销的桌面图标位置备份工具，在分辨率变化、显示器切换或图标意外重排后快速恢复布局。

## 功能需求

### P0：第一版 Demo

- 读取当前桌面图标名称及 X/Y 坐标
- 以物理像素记录虚拟桌面分辨率，并记录系统 DPI、显示器名称、设备标识、排列位置和备份时间
- 用户可命名备份，备份数据持久化到本地 JSON
- 按日期时间线展示历史备份、详情和图标数量，并明确手动、自动及恢复前备份的产生原因
- 在预览中对比当前桌面，显示图标变化和显示环境差异
- 将仍然存在的同名图标恢复到备份坐标，并可选择按显示器身份映射相对位置
- 恢复前自动创建当前布局的安全备份
- 支持删除历史备份
- 支持多选批量删除历史备份
- 提供操作成功、部分成功和失败反馈
- WinUI 3 Fluent 界面，支持浅色、深色及跟随系统主题
- 原生可折叠侧边导航
- 关于页面集中展示版本、本地存储位置、隐私与恢复保护信息
- 定时及事件触发自动备份，并限制自动备份保留数量
- 当前用户开机自启、自启时最小化、通知区域后台驻留及托盘快速备份
- 简体中文、English 与跟随系统语言切换
- 便携版和单项目 MSIX 打包，GitHub Actions 自动构建产物

### P1：可用性增强

- 备份布局缩略图与恢复前预览
- 分辨率/DPI 不一致时按比例适配
- 单独选择要恢复的图标
- JSON 备份导入、导出及版本迁移

### P2：完整产品能力

- 显示器缺失或替换时的交互式目标映射
- 显示器连接、断开和分辨率变化事件监听
- 跨设备布局迁移
- 高对比度、键盘导航和更多语言
- 固定自签证书用于本地正式包与 GitHub Release，普通 Actions Build 使用一次性测试证书；后续可继续评估 Microsoft Store 公共信任与自动更新

## 第一版技术方案

- UI：WinUI 3 / Windows App SDK 2.2，单窗口导航布局
- 运行方式：unpackaged 便携版或 single-project MSIX，x64、自包含 Windows App SDK
- 桌面访问：Win32 查找 `SHELLDLL_DefView/SysListView32`，通过跨进程内存读取图标文本与坐标
- 数据：`System.Text.Json`；便携版写入当前用户 LocalApplicationData，MSIX 版写入包专属 LocalFolder
- 恢复匹配：按图标显示名称匹配；重复名称按出现顺序处理
- 安全策略：任何人工恢复前都捕获并保存一份安全备份

## 里程碑

1. 工程骨架、主题资源与主窗口
2. 桌面图标捕获和系统环境读取
3. JSON 备份仓储及历史列表
4. 布局恢复、安全备份和结果反馈
5. 编译验证、使用说明和 Git 基线
6. 中英文资源、MSIX 打包脚本与 GitHub Actions

## Demo 验收标准

- 工程可通过 `dotnet build -p:Platform=x64` 编译
- 点击“立即备份”后，历史列表新增一条真实桌面布局记录
- 重启应用后历史记录仍存在
- 选择记录可执行恢复，且操作前生成安全备份
- 缺失图标不会导致整体恢复失败，界面会显示部分成功结果
- 便携版与 MSIX 均可由发布脚本生成，MSIX 清单包含中英文资源和启动任务
- 本地正式包与 Release 使用同一证书指纹；普通 Actions Build 使用一次性证书；Release 不包含私钥并提供 SHA-256 校验文件

## 已知限制

- Demo 仅验证 Windows Explorer 默认桌面，第三方桌面外壳不在支持范围
- 图标唯一性暂按显示名称判断；同名图标的稳定匹配将在后续增强
- 显示器匹配依赖 Windows 提供的设备标识；原显示器缺失时会回退到备份的绝对坐标
- 若启用了 Windows“自动排列图标”，Explorer 可能覆盖恢复的位置
